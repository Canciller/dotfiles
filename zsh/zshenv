function path {
	case ":$PATH:" in
	  *":$1:"*) :;; # already there
	  *) PATH="$1:$PATH";; # or PATH="$PATH:$new_entry"
	esac
}

path opt/cuda/bin
path /usr/local/bin

path $HOME/.local/bin
path $HOME/.bin

export PATH

export GDK_SCALE=1
export QT_AUTO_SCREEN_SCALE_FACTOR=1